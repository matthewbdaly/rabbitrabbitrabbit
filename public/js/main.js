$(document).ready(function () {
    'use strict';

    // Declare variables used
    var app, messages = [], socket, field, sendButton, content;

    app = new App();
    Backbone.history.start();
});
